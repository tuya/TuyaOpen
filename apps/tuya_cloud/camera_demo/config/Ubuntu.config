# camera_demo — Ubuntu (LINUX): P2P from demo_video.264 + local_store index (no MP4 mux)
# Usage:
#   cd apps/tuya_cloud/camera_demo
#   tos.py config choice -c Ubuntu.config
#   tos.py build
# Run from app dir so ./demo_video.264 and ./media are found.
#
# Media path: filesystem H264 file (src/tuya_ipc_demo.c LINUX branch)
# Playback: RingBuffer + local_store day index (PB frame send)

CONFIG_BOARD_CHOICE_LINUX=y
CONFIG_BOARD_CHOICE_UBUNTU=y

CONFIG_ENABLE_TUYA_P2P=y
CONFIG_ENABLE_WIRED=y
# CONFIG_ENABLE_WIFI is not set

CONFIG_ENABLE_LOCAL_STORE=y
CONFIG_LOCAL_STORE_ROOT="./media"
CONFIG_LOCAL_STORE_REC_SUBDIR="rec"
CONFIG_LOCAL_STORE_PATH_MAX=256
CONFIG_ENABLE_IPC_RING_BUFFER=y
CONFIG_CAMERA_DEMO_PB_SEED=y

CONFIG_ENABLE_MBEDTLS_SSL_MAX_CONTENT_LEN=4096

# Headless cloud/P2P demo (no LVGL / SDL window)
# CONFIG_ENABLE_SDL_DISPLAY is not set
# CONFIG_ENABLE_KEYBOARD_INPUT is not set
