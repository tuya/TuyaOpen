# 推送代码到GitHub - 需要填写的信息

## 🔐 推送时需要填写的信息

当您运行 `git push` 命令时，系统会提示您输入：

### 1. Username（用户名）
填写您的 **GitHub 用户名**（不是邮箱）

例如：
```
Username: your_github_username
```

### 2. Password（密码）
**⚠️ 重要：这里不能填您的GitHub账户密码！**

您需要填写 **GitHub Personal Access Token**

---

## 📝 如何获取 Personal Access Token

### 步骤1：访问Token设置页面
打开浏览器，访问：
```
https://github.com/settings/tokens
```

### 步骤2：生成新Token
1. 点击 **"Generate new token"** 按钮
2. 选择 **"Generate new token (classic)"**

### 步骤3：配置Token
- **Note（备注）**：填写 `TuyaOpen Push`（任意名称）
- **Expiration（过期时间）**：选择 `90 days` 或 `No expiration`
- **Select scopes（选择权限）**：
  - ✅ 勾选 **`repo`** 下的所有选项
    - ✅ repo (Full control of private repositories)
    - ✅ workflow (Update GitHub Action workflows)
    - ✅ write:packages (Upload packages to GitHub Package Registry)
    - ✅ delete:packages (Delete packages from GitHub Package Registry)

### 步骤4：生成并复制Token
1. 滚动到底部，点击 **"Generate token"**
2. **⚠️ 立即复制Token**（只显示一次！）
3. Token格式类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

## 🚀 推送命令

在终端运行：
```bash
cd /Users/wuchang/Documents/TuyaOpen
git push -u origin feature/led-pwm-control
```

当提示时：
- **Username**: 输入您的GitHub用户名
- **Password**: 粘贴刚才复制的Token（不是密码！）

---

## ❓ 常见问题

### Q: 提示 "Authentication failed"
**A**: 检查：
- Token是否正确复制（没有多余空格）
- Token权限是否包含 `repo`
- Token是否已过期

### Q: 提示 "Permission denied"
**A**: 您可能没有直接推送到 `tuya/TuyaOpen` 的权限，需要：
1. Fork仓库到您的账户
2. 推送到您自己的fork

### Q: 如何查看已保存的凭据？
**A**: 在macOS上：
- 打开 "钥匙串访问" 应用
- 搜索 "github.com"
- 可以查看或删除保存的凭据

---

## 📌 当前状态
- ✅ 分支已创建：`feature/led-pwm-control`
- ✅ 代码已提交：提交ID `60a453bc`
- ⏳ 等待推送：需要GitHub认证

