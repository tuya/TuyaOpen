# 代码更新到GitHub的流程

## 📝 标准更新流程

### 步骤1：查看更改状态
```bash
cd /Users/wuchang/Documents/TuyaOpen
git status
```

### 步骤2：添加更改的文件
```bash
# 添加所有更改的文件
git add .

# 或者添加特定文件
git add apps/tuya.ai/your_chat_bot_led_ctl/src/某个文件.c
```

### 步骤3：提交更改
```bash
git commit -m "描述您的更改内容"
```

### 步骤4：推送到远程仓库
```bash
# 推送到 tuyaopenAILED 仓库的当前分支
git push tuyaopenAILED feature/led-pwm-control

# 或者如果已经设置了跟踪
git push
```

---

## 🚀 快速更新命令（一条命令）

```bash
cd /Users/wuchang/Documents/TuyaOpen && \
git add . && \
git commit -m "更新LED PWM控制功能" && \
git push tuyaopenAILED feature/led-pwm-control
```

---

## 📋 详细说明

### 1. 查看更改
```bash
git status                    # 查看所有更改的文件
git diff                      # 查看未暂存的更改内容
git diff --cached             # 查看已暂存但未提交的更改
```

### 2. 添加文件到暂存区
```bash
git add .                     # 添加所有更改
git add 文件路径              # 添加特定文件
git add -u                    # 只添加已跟踪文件的更改（不包括新文件）
git add -A                    # 添加所有更改（包括删除的文件）
```

### 3. 提交更改
```bash
# 简单提交
git commit -m "修复LED亮度控制bug"

# 详细提交信息
git commit -m "fix: 修复LED亮度控制问题

- 修复了长按按键时亮度调整异常的问题
- 优化了PWM占空比计算逻辑
- 更新了README文档"
```

### 4. 推送到远程
```bash
# 推送到特定远程仓库和分支
git push tuyaopenAILED feature/led-pwm-control

# 如果已设置上游分支跟踪
git push

# 强制推送（谨慎使用！）
git push -f tuyaopenAILED feature/led-pwm-control
```

---

## 🔄 工作流程示例

### 示例1：修改了单个文件
```bash
# 1. 查看更改
git status

# 2. 添加文件
git add apps/tuya.ai/your_chat_bot_led_ctl/src/pwm_led_ctrl.c

# 3. 提交
git commit -m "优化LED PWM控制逻辑"

# 4. 推送
git push tuyaopenAILED feature/led-pwm-control
```

### 示例2：修改了多个文件
```bash
# 1. 查看所有更改
git status

# 2. 添加所有更改
git add .

# 3. 提交
git commit -m "更新LED控制功能和文档

- 优化了触摸按键响应速度
- 修复了亮度控制范围问题
- 更新了README中的配置说明"

# 4. 推送
git push tuyaopenAILED feature/led-pwm-control
```

---

## 🛠️ 常用Git命令

### 查看历史
```bash
git log --oneline            # 查看提交历史（简洁）
git log                      # 查看详细提交历史
git log -5                   # 查看最近5次提交
```

### 撤销操作
```bash
# 撤销工作区的更改（未暂存）
git restore 文件路径

# 撤销已暂存但未提交的更改
git restore --staged 文件路径

# 修改最后一次提交
git commit --amend -m "新的提交信息"
```

### 查看远程仓库
```bash
git remote -v                # 查看所有远程仓库
git remote show tuyaopenAILED  # 查看远程仓库详情
```

---

## ⚠️ 注意事项

1. **提交前检查**：使用 `git status` 和 `git diff` 确认更改正确
2. **提交信息清晰**：使用有意义的提交信息，方便后续查看历史
3. **不要强制推送**：除非确定，否则避免使用 `git push -f`
4. **定期拉取**：如果多人协作，推送前先拉取最新代码

---

## 📌 当前配置

- **远程仓库**: `tuyaopenAILED` → `git@github.com:robeortZ/tuyaopenAILED.git`
- **当前分支**: `feature/led-pwm-control`
- **推送方式**: SSH (已配置)

---

## 🎯 一键更新脚本

可以创建一个简单的更新脚本：

```bash
#!/bin/bash
cd /Users/wuchang/Documents/TuyaOpen
git add .
git commit -m "$1"
git push tuyaopenAILED feature/led-pwm-control
```

使用方法：
```bash
./update.sh "更新LED控制功能"
```

